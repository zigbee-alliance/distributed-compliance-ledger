---
# Copyright 2022
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#      http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

version: "2"

linters:
  # Explicit allowlist. Avoid `default: all` so future golangci-lint releases
  # don't silently enable new noisy linters on upgrade.
  default: none
  enable:
    # --- Core correctness (real bugs) ---
    - bodyclose
    - contextcheck
    - durationcheck
    - errcheck
    - errchkjson
    - errorlint
    - fatcontext
    - gocheckcompilerdirectives
    - gochecksumtype
    - govet
    - ineffassign
    - makezero
    - nilerr
    - nilnesserr
    - noctx
    - reassign
    - rowserrcheck
    - spancheck
    - sqlclosecheck
    - staticcheck
    - unused
    - wastedassign

    # --- Test correctness ---
    - ginkgolinter
    - testableexamples
    - testifylint
    - thelper
    - tparallel
    - usetesting

    # --- API / stdlib usage ---
    - asasalint
    - canonicalheader
    - exptostd
    - importas
    - loggercheck
    - mirror
    - musttag
    - nosprintfhostport
    - promlinter
    - protogetter
    - sloglint
    - usestdlibvars
    - zerologlint

    # --- Style / quality ---
    - asciicheck
    - bidichk
    - decorder
    - dogsled
    - dupword
    - embeddedstructfieldcheck
    - gocritic
    - gocyclo
    - goheader
    - goprintffuncname
    - grouper
    - iface
    - interfacebloat
    - iotamixing
    - misspell
    - nakedret
    - nestif
    - nilnil
    - nlreturn
    - nolintlint
    - tagalign
    - unconvert
    - whitespace

    # --- Niche / dependency-specific (no findings today, kept for safety nets) ---
    - arangolint
    - clickhouselint
    - gomodguard
    - gosmopolitan
    - unqueryvet

  settings:
    staticcheck:
      # Drop "quick fix" suggestions (QF*) and stylecheck rules (ST*).
      # ST* used to be a separate `stylecheck` linter in v1 — kept disabled
      # here to match the original team policy.
      checks:
        - all
        - -QF*
        - -ST*
    goheader:
      # Match the canonical DSR Corporation Apache-2.0 header. The year is a
      # regex so existing 2020/2021/2022/... headers all match. Template text
      # lives in .golangci-goheader-template.txt
      values:
        regexp:
          year: "20\\d{2}"
      template-path: .golangci-goheader-template.txt
    govet:
      # Disable the modernization "inline" analyzer — it nags about replacing
      # ioutil/reflect constants with newer equivalents. Style, not bugs.
      disable:
        - inline

  exclusions:
    # Skip auto-generated files (e.g. *.pb.go from protoc).
    generated: lax
    rules:
      - path: _test\.go$
        linters:
          - testifylint
          - dupword
      - path: ^integration_tests/.*/helpers\.go$
        linters:
          - testifylint
          - dupword

# In golangci-lint v2 formatters live in their own block (separate from
# `linters:`). Keep this minimal: gofmt and goimports are the existing repo
# baseline; gofumpt/gci can be added later if the stricter style will be needed.
formatters:
  enable:
    - gofmt
    - goimports
  exclusions:
    generated: lax
